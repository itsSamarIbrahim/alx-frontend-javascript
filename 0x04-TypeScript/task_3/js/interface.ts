// Define the RowID type as a number
export type RowID = number;

// Define the RowElement interface with firstName, lastName, and an optional age
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}
